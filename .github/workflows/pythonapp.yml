# This workflow will install Python dependencies then try to run the `Entry.py` file in `src` folder.
name: Python Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Install dependencies
      run: |
        apt-get update -qq
        apt-get install -y -qq gcc python3 python3-dev python3-pip
        echo "ls"
        ls
        echo "pwd"
        pwd 
        echo "before cd command"
        cd ./src
        echo "Where am I?"
        ls
        echo "here I am"
        pip3 install --quiet -r requirements.txt
        echo "potato"
    - name: Execute scripts for testing:
      run: |
        cd ./src
        ls
        python3 Entry.py
